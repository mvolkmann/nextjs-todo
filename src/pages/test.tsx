// This is an example of a page that is defined under
// the "pages" directory instead of the "app" directory.
// Rather than creating a directory named after the page
// and placing a "page.tsx" file inside it,
// we can create a ".tsx" file named after the page.
// Browse localhost:3000/test render this.
export default function TestPage() {
  return (
    <section>
      <h1>Test Page</h1>
    </section>
  );
}
